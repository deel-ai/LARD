GESDataset
=================================================================

generate poses
--------------------------------------------------

Here is the ''generate_landing_poses()'' function:

.. autofunction:: GESDataset.GESDataset.generate_landing_poses

convert poses into an esp format file
--------------------------------------------------

to do so we use ''create_scenario()'' function:

.. autofunction:: GESDataset.GESDataset.create_scenario